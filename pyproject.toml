[project]
name = "outreach-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "argon2-cffi>=25.1.0",
    "fastapi[standard]>=0.139.0",
    "pyjwt[crypto]>=2.13.0",
    "redis>=8.0.1",
    "sqlmodel>=0.0.32",
]
